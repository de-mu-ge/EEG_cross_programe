# 复制为 .env 后按需修改（.env 已被 gitignore，不会提交）

# 真实模型权重路径（相对 python-service 根目录或绝对路径）
MODEL_PATH=models/eeg_model.pt

# 推理设备：cpu / cuda
DEVICE=cpu

# 1=强制演示模式（不解析文件，返回确定结果 happy/0.92）
# 0=自动判断：模型文件与依赖可用则真实推理，否则演示兜底
DEMO_MODE=0
