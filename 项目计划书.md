# NEURO-AI · 多模态脑机接口情绪识别系统

**项目计划书**

---

## 目录

1. [项目概述](#一项目概述)
2. [市场与需求分析](#二市场与需求分析)
3. [技术方案与现状盘点](#三技术方案与现状盘点)
4. [创新点与竞争优势](#四创新点与竞争优势)
5. [项目实施计划（里程碑）](#五项目实施计划里程碑)
6. [团队分工](#七团队分工)
7. [商业模式](#八商业模式)
8. [风险分析与对策](#九风险分析与对策)
11. [附录：仓库现状清单](#附录仓库现状清单)

---

## 一、项目概述

### 1.1 项目背景

情绪识别是脑机接口（BCI）与情感计算领域的核心任务。当前行业存在两个相互纠缠的难题：

1. **脑电信号（EEG）个体差异极强，难以泛化。** 同一种情绪在不同个体头皮上的脑电空间分布、节律特征差异巨大，导致"在 A 个体上训练的模型迁移到 B 个体上准确率骤降"。这是单模态 EEG 方案在真实工程环境中失效的根因。
2. **视觉信号易受干扰，稳定性差。** 光照、遮挡、姿态、佩戴物等环境因素会显著污染面部/肢体视觉特征，单一依赖视觉的方案在矿井、救援等恶劣光照条件下不可靠。

单模态方案各自有硬伤，而**多模态信息融合**正是弥补单一模态短板、提升跨个体泛化能力与鲁棒性的行业公认方向。

### 1.2 产品定义

本项目构建一套**"多模态感知 + 大模型决策端 + 量化边缘部署"**的情绪识别系统：

- **感知端**：EEG 脑电、面部/场景视觉、音频三类信号并行采集；
- **决策端**：各单模态模型先做初步分类，再由**大模型作为决策大脑**，结合各模态置信度、个体差异度与场景上下文做最终情绪判别，并输出可解释依据；
- **部署端**：模型经 ONNX 导出与 int8 量化压缩后，以 **C 语言边缘推理引擎**运行于无云端依赖的受限设备，保障矿井、救援等场景的实时性与数据私密性。

### 1.3 项目愿景

> 通过**基于大模型决策端的分类方法**与**量化后的边缘部署方案**，构建一套在工程化领域具有实际作用的**开源系统**。

不以实验室精度为唯一目标，而以"**在真实受限环境中能稳定跑起来**"为核心指标。

### 1.4 目标用户与应用场景

| 场景 | 具体需求 | 对应技术价值 |
| --- | --- | --- |
| 矿井作业 | 疲劳、应激、情绪异常实时监测 | 边缘部署、无网络依赖、脑电为主抗环境干扰 |
| 灾难救援 | 被困者/救援人员的心理应激评估 | 便携设备、实时推理、多模态兜底 |
| 医学侦测 | 抑郁、焦虑等情绪的辅助筛查 | 跨个体泛化、可解释决策辅助医生判断 |
| 心理健康 | 数字疗法、情绪日记、干预反馈 | 轻量部署、长期跟踪、开源可定制 |

---

## 二、市场与需求分析

### 2.1 行业痛点（"为什么现在做"）

1. **跨个体泛化差**：EEG 数据个体差异导致模型"换个人就失效"，制约脑机接口从实验室走向产业。
2. **单模态准确率瓶颈**：EEG 单模态、视觉单模态均有天花板，多模态融合是突破方向。
3. **视觉不稳定**：真实场景光照/遮挡多变，视觉单独使用不可靠。
4. **部署约束**：情绪识别涉及隐私数据（脑电、人脸），很多场景不允许云端上传；且矿区/灾区网络不可用。
5. **传统方法落后**：手工特征 + SVM 等传统分类方法依赖专家经验，跨个体与抗噪能力弱。

### 2.2 市场规模简述

情绪识别是情感计算产业的核心入口，下游覆盖人机交互、医疗健康、工业安全、教育、娱乐等。随着脑机接口设备（干电极头环等）成本下降，**边缘端实时情绪识别的需求快速增长**；而目前市场上几乎没有"多模态 + 大模型决策 + 边缘部署 + 开源"一体化方案。

### 2.3 竞争对手分析

| 对比维度 | 传统算法分类（特征+SVM等） | 单模态深度学习 | **本项目（多模态+大模型决策端+边缘部署）** |
| --- | --- | --- | --- |
| 跨个体泛化 | 弱 | 弱 | 强（多模态互补 + 大模型上下文校准） |
| 抗干扰能力 | 中 | 弱（视觉易受光照/遮挡影响） | 强（EEG 为主 + 视觉兜底 + 决策级融合） |
| 可解释性 | 低 | 低 | 高（大模型输出推理依据） |
| 部署形态 | 可边缘化但精度差 | 多为云端/需要 GPU | 量化 + C 引擎，可在受限设备离线运行 |
| 开放性 | — | 多为闭源 | 开源系统 |

**结论**：本项目不与单模态方案正面拼"单一指标"，而以"工程可部署 + 跨个体可用 + 可解释"的综合能力形成差异化。

---

## 三、技术方案与现状盘点

### 3.1 总体架构（五层）

```
┌─────────────────────────────────────────────────────────────┐
│  应用层   Java Spring Boot 业务后端 + MySQL + Web 演示前端      │
├─────────────────────────────────────────────────────────────┤
│  边缘部署层   ONNX int8 量化 + C 推理引擎（C/ONNX）+ 边缘设备    │
├─────────────────────────────────────────────────────────────┤
│  决策融合层   大模型决策端（LLM）：置信度 + 个体差异 + 场景推理    │
├─────────────────────────────────────────────────────────────┤
│  特征提取层   EEG 模型 / 视觉模型(3D Conv) / 音频模型           │
├─────────────────────────────────────────────────────────────┤
│  感知层      脑电采集 · 视频采集 · 音频采集                     │
└─────────────────────────────────────────────────────────────┘
```

### 3.2 现状盘点（结合仓库实测）

| 模块 | 状态 | 说明与位置 |
| --- | --- | --- |
| DEAP EEG 数据管线 | ✅ 完成 | 32 EEG通道 × 8064 采样点（63s@128Hz），切 4 段各 2016 点，`EEG/Dataload` |
| EEG 三通道模型训练/推理 | ✅ 完成 | valence/arousal/dominance 三模型，FastAPI 推理 `EEG/out/predict` |
| EAV 视频模型训练/推理 | ✅ 完成 | 60 帧 480×640×3 → 3D 卷积 → 二分类，`Video/一站式推理架构.py`、`Video/out/app` |
| ONNX 导出 + int8 量化 | ✅ 完成 | Base视频模型已导出 （opset18，动态量化） |
| Java 业务后端 | ✅ 完成 | 上传文件 → 转发 Python 推理 → 结果入库，`java/backend`（Spring Boot + MyBatis-Plus + MySQL） |
| 多模态融合层 | ❌ 待开发 | EEG/Video 目前是**两个独立 FastAPI 服务**，尚无融合逻辑 |
| 大模型决策端 | ❌ 待开发 | 产品愿景核心，尚未实现 |
| C 边缘推理引擎 | ❌ 待开发 | `C/ONNX` 目前为空目录，仅完成了 ONNX 量化，无 C 端代码 |
| 音频模态 | ⚠️ 未启用 | README 表明 EAV 含音频，代码目前只用 video |
| 前端 | 初步投入使用 | 前端使用了Vue框架，目前主要调用后端接口的方法实现基础功能，并提供本项目个性化的人机交互 |

### 3.3 多模态融合方案

采用**"特征级 + 决策级"双轨**设计，便于消融对比：

- **特征级融合**：EEG 特征、视频时序特征、音频特征在时间轴对齐后拼接，输入融合分类头；
- **决策级融合**：各单模态模型先独立输出分类与置信度，将结果向量（top-k 类别 + 置信度 + 个体差异度）交给大模型决策端做最终判别；
- **个体差异校准**：引入"个体差异度"度量（如特征分布的域距离），作为大模型推理的上下文，缓解跨被试泛化问题。

> 创新点：不追求"把多模态塞进一个黑盒"，而是让**每个模态各自可控、可解释、可替换**，最后用大模型做决策融合——这也是工程上可维护性的来源。

### 3.4 大模型决策端设计

计划采用模块化的设计，尽可能多的利用项目应用场景中的数据

### 3.5 边缘量化部署方案

**技术路线**：`PyTorch 训练 → ONNX 导出 → 动态/静态 int8 量化 → C 推理引擎 → 边缘设备（树莓派 / Jetson / RK 系列）`

- **已完成**：视频模型已走通 ONNX 导出 + int8 动态量化（见 `Video/src/量化.py`，`Video/src/model*.onnx`）；
- **待完成**：`C/ONNX` 目录下实现 C 端推理（基于 onnxruntime C API 或自研算子），提供与 Python 端一致的输入输出接口，并接入边缘设备实测；
- **指标**：以模型体积压缩、推理耗时、内存占用为验收指标，与 Python 端做一致性比对。

### 3.6 应用层

- **后端**：现有 `java/backend` 已支持 `.dat` 上传、同步分析、结果查询（状态机 WAITING→RUNNING→SUCCESS/FAILED）。后续扩展为多模态上传（脑电 + 视频 + 音频），记录与展示融合后的决策结果。
- **前端**：开发 Web 演示页，目前可视化展示单模态分类 ，支撑比赛演示。

---

## 四、创新点与竞争优势

1. **跨个体泛化机制**：个体差异度量 + 多模态互补，直接针对"脑电难泛化"这一行业根因。
2. **量化 + C 边缘部署闭环**：从训练到 int8 量化再到 C 推理引擎的全链路，可在无网络、弱算力的真实环境运行，契合矿井/救援等场景。
3. **开源工程化**：公开数据集（DEAP/EAV）复现、清晰分层架构、可运行服务，面向工程而非仅论文指标。
4. **对抗竞争对手**：相比单模态（泛化差/不稳）与传统算法（依赖手工特征），以"工程可部署 + 跨个体可用 + 可解释"形成综合壁垒。

---

## 五、项目实施计划

### 阶段 1：多模态融合

- **目标**：打通 EEG + 视频 + 音频三模态数据对齐与融合，产出可对比的融合效果。
- **关键任务**
  - 启用 EAV 音频模态：提取音频特征（如梅尔频谱）并接入训练/推理管线；
  - 定义统一的数据对齐格式（时间轴 / 采样窗口 / 标签对齐）；
  - 实现特征级融合与决策级融合两套基线；
  - 完成**消融实验**（单模态 vs 特征融合 vs 决策融合），量化"融合增益"。
- **验收标准**：融合模型在跨被试划分下较最佳单模态有可量化的指标提升；消融表可作为比赛数据支撑。

### 阶段 2：工程化与开源

- **目标**：把整套系统整合为可演示、可复现、可开源的项目。
- **关键任务**
  - Java 后端扩展为多模态上传与分析；
  - 开发 Web 演示前端（可视化单模态结果 + 决策依据）；
  - 完善 README、部署文档、数据集授权说明、一键启动脚本；
  - 整理开源仓库（目录规范、LICENSE、示例数据）。
- **验收标准**：一条命令可从空环境启动完整演示链路；开源仓库结构清晰。



### 阶段 3：边缘量化部署

- **目标**：在边缘设备上跑通 C 推理，形成"量化 + 部署"闭环。
- **关键任务**
  - `C/ONNX` 目录下实现 C 推理引擎（onnxruntime C API 起步）；
  - 静态量化、模型剪枝/蒸馏调研，压缩模型体积；
  - 在边缘设备（树莓派 / Jetson 等）实测：推理耗时、内存、功耗；
  - 与 Python 端做**输出一致性比对**，保证部署不损失精度。
- **验收标准**：边缘设备离线实时推理成功，模型体积与耗时较原始模型显著下降。



---

## 六、团队分工

全栈开发师 : 井溢杨

计算机视觉工程师 : 张舜

测试工程师 : 朱智诚

服务器运维 : 于谦

## 七、商业模式

本项目以**开源为核心**，面向 B 端工程化场景变现：

1. **开源核心（免费）**：基础模型、推理服务、示例代码，沉淀社区与生态；
2. **企业级部署服务**：针对矿井/医疗等场景的边缘设备适配、定制优化（盈利点）；
3. **定制化模型训练**：为企业提供基于私有数据的模型训练与个体适配服务；
4. **硬件联动**：与脑电采集设备（头环/头带）厂商合作，预装推理引擎（生态盈利）。



---

## 八、风险分析与对策

| 风险 | 等级 | 应对措施 |
| --- | --- | --- |
| 多模态融合效果不一定优于最佳单模态 | 高 | 坚持消融实验，用数据说话；决策级融合兜底（即使融合无增益，解释性与鲁棒性仍是价值） |
| EAV 数据集在外部路径，数据不全/授权问题 | 中 | 及时整理/申请公开数据集；在开源时明确数据集授权与版权说明 |
| 边缘设备算力不足，量化后精度损失 | 中 | 量化 + 蒸馏双路线；选择 Jetson/RK 等中端边缘设备；以一致性比对保障 |
| 网络安全风险                            | 中   | 目前仅通过内网穿透提供远程服务                               |
| 项目进度与比赛时间冲突 | 中 | 阶段 1–2 优先保证"可演示的端到端链路"，再补强部署 |
| 开源后核心壁垒被复制 | 低 | 以工程经验、场景适配、数据与部署服务建立壁垒，模型本身开放 |

## 

---

## 附录：仓库现状清单

```
├── README.md              数据输入端说明（deap / eav）
├── C/ONNX/                边缘部署占位（空，待实现 C 推理引擎）
├── Cpython/
│   ├── equirements.txt    Python 依赖（torch 2.11 + fastapi + onnxruntime 待补）
│   ├── data/dataset/      DEAP 数据集
│   └── main/
│       ├── EEG/           脑电：Dataload / model / src/train / out/predict(FastAPI)
│       └── Video/         视频：Dataload / model / src/一站式推理架构.py / src/量化.py / out/app(FastAPI)
├── java/backend/          Spring Boot 3.3.5 + MyBatis-Plus + MySQL（上传→转发→入库）
└── txt/进程/               进程记录（空）
```

**已就位能力**：DEAP EEG 三模型推理、EAV 视频 3D 卷积推理、ONNX int8 量化、Java 后端上传分析闭环、服务器部署。

**待建设能力**：音频模态、多模态融合、大模型决策端、C 边缘推理引擎、Web 前端、开源整理。
